build/
.claude/

# A JS surface here (the ACP experiments) installs its own tree, and one of them
# vendors a 292MB `claude` binary — well past the size jj will snapshot. Ignored
# once at the root rather than per directory.
node_modules/
